function principal (){}
	do{
	var respuesta = prompt("¿Qué deseas hacer, cifrar 1 o descifrar 2?");
		if(respuesta !== "") //Si no es blanco comienza la decisión.
	if(respuesta === 1){
		 cipher ();
	}else if(respuesta === 2){
		 decipher ();
	} else {
		alert("ingresa una opción valida");
		}while (respuesta === "" ||(respuesta !="1" && respuesta != "2"));
   	} 

function cipher (){} 
 	var sentence = prompt("¿Qué frase quieres cifrar?"))
	    string.charCodeAt()//como obeter el codigo ASCII de la frase.
	if(x - 65 + 33 + 26)%26 + 65 // con está formula se obtendrá la posición según el parametro de desplazamiento (33).
 	return String.fromCharCode()// pasar la posición a letra.


function decipher (){}
var position = prompt("¿Qué frase quieres decifrar")
	(x - 33 + 26) % 26 // formula para decifrar.
	(x - 65 + 33)%26 + 65
	string.charCodeAt ()
	return String.fromCharCode

 	 
 	 }
 	 if(""|| number){ // No debe haber especios en blancos o números.
 	 	alert("¡No debes ingresar frases con espacios o números!");
 	 }